#!/bin/sh
# Synara provider shim: forwards to synara-account-launcher (plan section 21).
dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
SYNARA_LAUNCHER_SHIM=grok exec bun "$dir/../src/launcher.ts" "$@"
